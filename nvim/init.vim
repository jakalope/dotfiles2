set expandtab   " use spaces instead of tabs

" default to 4 spaces per tabstop
set tabstop=4
set shiftwidth=4
set softtabstop=4

call plug#begin('~/.config/nvim/plugged')
Plug 'williamboman/mason.nvim'
call plug#end()

